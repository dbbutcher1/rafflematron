<div class = "container">
<%= form_for @raffle, :html => { :class => "well" } do |f| %>
  <%= f.label :name, "Name of raffle: " %>
  <%= f.text_field :name %> <br />
  <%= f.label :num_prizes, "Prizes" %>
  <%= f.text_field :num_prizes %>
  <%= f.label :num_entries, "Entries per person" %>
  <%= f.text_field :num_entries %> <br/>
  <%= f.hidden_field :id %>
  <!--<%= hidden_field_tag(:raffle_id, @raffle.id) %>-->
  <%= f.submit "Save" %>
<% end %>
</div>
